<Window x:Class="HPToy.Win.Dialogs.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Height="160" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Icon="/Assets/hptoy.png">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="PromptText" Grid.Row="0" Margin="0,0,0,8"/>
        <TextBox x:Name="InputBox" Grid.Row="1" Margin="0,0,0,12"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="OkBtn" Width="88" Margin="0,0,8,0" IsDefault="True" Click="Ok_Click"/>
            <Button x:Name="CancelBtn" Width="88" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
