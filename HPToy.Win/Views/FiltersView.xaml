<UserControl x:Class="HPToy.Win.Views.FiltersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="16" MaxWidth="560">
            <CheckBox x:Name="PeqBypassCheck" Margin="0,0,0,12"
                      Checked="PeqBypass_Changed" Unchecked="PeqBypass_Changed"/>
            <ComboBox x:Name="BiquadCombo" Margin="0,0,0,12" SelectionChanged="BiquadCombo_SelectionChanged"/>
            <CheckBox x:Name="EnabledCheck" Margin="0,0,0,8"
                      Checked="Enabled_Changed" Unchecked="Enabled_Changed"/>
            <ComboBox x:Name="TypeCombo" Margin="0,0,0,12" SelectionChanged="TypeCombo_SelectionChanged"/>
            <TextBlock x:Name="FreqTitleLabel"/>
            <DockPanel Margin="0,4,0,12">
                <TextBlock x:Name="FreqLabel" Width="80" VerticalAlignment="Center"/>
                <Slider x:Name="FreqSlider" Minimum="20" Maximum="20000" ValueChanged="FreqSlider_ValueChanged"/>
            </DockPanel>
            <TextBlock x:Name="QTitleLabel"/>
            <DockPanel Margin="0,4,0,12">
                <TextBlock x:Name="QLabel" Width="80" VerticalAlignment="Center"/>
                <Slider x:Name="QSlider" Minimum="0.1" Maximum="10" ValueChanged="QSlider_ValueChanged"/>
            </DockPanel>
            <TextBlock x:Name="DbTitleLabel"/>
            <DockPanel Margin="0,4,0,12">
                <TextBlock x:Name="DbLabel" Width="80" VerticalAlignment="Center"/>
                <Slider x:Name="DbSlider" Minimum="-24" Maximum="24" ValueChanged="DbSlider_ValueChanged"/>
            </DockPanel>
            <StackPanel Orientation="Horizontal">
                <Button x:Name="AddLpBtn" Margin="4" Click="AddLp_Click"/>
                <Button x:Name="AddHpBtn" Margin="4" Click="AddHp_Click"/>
                <Button x:Name="RemoveFilterBtn" Margin="4" Click="RemoveFilter_Click"/>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>
